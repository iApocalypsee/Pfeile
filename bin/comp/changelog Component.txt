/**
 * Übergeordnete Basisklasse von allen klickbaren Elementen im Programm.
 * 
 * <br><br>
 * <b>4.1.2014:</b> Eltern-Kind-Beziehung zwischen Components programmiert: relative
 * Koordinaten zu anderen Components möglich. Ich erhoffe mir davon eine einfachere Erstellung von
 * anderen Components wie z.B. Listen. <br><br>
 * <b>12.1.2014:</b> Alle Felder jetzt <code>private</code> anstatt <code>protected</code>. Wieso?
 * Der Zugriff auf diese Felder soll nun über die entsprechenden Getter und Setter erfolgen, damit
 * auch alles andere, was auf <code>x, y, width, height</code> bezogen ist, auch aktualisiert wird.
 * Hauptsächlich deswegen, weil ich keinen buggy Code schreiben will, der auch noch schwer zu untersuchen ist.
 * <br><br>
 * <b>15.1.2014:</b> Fix, der <b>alle</b> Components betrifft; jeder MouseListener, der der Component
 * hinzugefügt wird, muss einen Check beinhalten, der überprüft, ob die Maus wirklich auch im Component 
 * drin ist. <b>Sehr schwer auffindbarer Bug, wenn man vergisst diesen Check durchzuführen.</b>
 * <br>
 * Er geht in etwa so: <br><br>
 * <code> if(component.getBounds().contains(e.getPoint()) <br>
 * { <br>
 * (do something)... <br>
 * } </code> 
 * <br>
 * Fix 2: Die Bounds von Components, deren <code>{@link #parent} != null</code>, bleiben absolut
 * und werden nicht an relative Koordinaten angepasst. <br><br>
 * <b>17.1.2014:</b> Bugfixes. Anzahl von Vertices, die eine Component aufnehmen kann, ist
 * standardmäßig auf <code>50 + 50 = 100</code> begrenzt. (siehe auch 
 * {@link Component#POLYGON_BUFFER_CAPACITY}) 
 * <br><br>
 * <b>20.1.2014:</b> MouseListener-Funktionalität entschlackt. Der obere Check müsste
 * nicht mehr notwendig sein.
 * <b>8.2.2014:</b>
 * <p> Bugfixes und Bugfixes:
 * <ul>
 * <li> Die Berechnung der Bounds ist jetzt wieder synchron mit den Setter-Funktionen.
 * <li> Polygon wird jetzt wieder anstatt Rectangle verwendet (es funktioniert jetzt)
 * 
 * @version 20.1.2014
 *
 */